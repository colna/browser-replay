{
  "name": "browser-replay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "录制浏览器操作并回放的 Chrome 扩展（MV3），可导出 JSON，选择器不使用 class",
  "scripts": {
    "test": "node test/run-all.mjs",
    "test:selector": "node test/run-all.mjs --selector-only",
    "test:e2e": "node test/e2e.mjs",
    "test:e2e:headful": "node test/e2e.mjs --headful"
  }
}
